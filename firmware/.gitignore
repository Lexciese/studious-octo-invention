.pio/
.tox/
.pioenvs/
.piolibdeps/
.clang_complete
.gcc-flags.json
-Thumbs.db
.vscode/
!.vscode/extensions.json

# Local config with hotspot password — keep out of git
src/config.h

# LittleFS data dir — populated by scripts/build-dashboard.sh, flashed with
# `pio run -t uploadfs`. Contents are a build artifact (copy of dashboard/out/).
data/*
!data/.gitkeep
